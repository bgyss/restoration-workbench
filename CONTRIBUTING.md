# Contributing

Keep source captures, extracted audio/video, model weights, and private review assets outside
Git. Add deterministic media-free tests for command construction, manifests, path policy, and
timeline invariants. Preserve the sample-first and human-approval gates. Run `ruff check
comfyui_restoration scripts tests` and `python -m pytest -q` before submitting a change.

